  //make switch case functions to create logo

function renderColor(color) {
  switch (color) {
    case '':
      return '';
  };
}

function renderShape(shape) {
  switch (shape) {
    case '':
      return 'circle';
    case 'square':
      return '';
    case 'triangle':
      return '';    
  };
}

function renderShapeColor(shapeColor) {

}